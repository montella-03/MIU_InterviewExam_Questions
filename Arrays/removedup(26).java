public static int removeduplicate(int[] array){
  set<integer> set = new HashSet<>();
  for(i=0;i<array.length;i++){
        set.add(array[i]
        }
        set.forEach(e-> sout(e));
                
                
//
                
        
